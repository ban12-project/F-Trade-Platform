# 按需 Browser Sandbox 迁移（#332）

用户于 2026-09-08 明确要求：Browser Sandbox 仅在需要时启动，不常驻。
本迁移延续 #322 / PR #323 的节点、账号与任务授权约束，最终验收暂缓。

## 生命周期契约

- 开启计算的触发仅为人工接管请求或已获授权且实际到期的业务任务。节点列表刷新、状态查询、保活与空队列轮询不得启动或恢复 Sandbox。
- 平台负责按需调度；不能把当前常驻 Agent 的空轮询直接搬进长期运行的 Sandbox。
- 任务完成、用户关闭接管、无用户连接超时或授权撤销后，停止浏览器，再停止整个 Sandbox。运行超时是兜底，不是正常回收机制。
- 没有任务时不得延长会话。并发启动、停止和恢复必须以数据库中的操作标识和状态串行协调；模糊结果先查已有实例，不能创建替代实例并丢弃旧状态。
- 账号独立的 profile 必须在停止前一致落盘；恢复要重启 Docker 和 Agent，旧租约按现有恢复协议终止或确认，不恢复未知发布尝试。
- 列表状态只能使用不恢复会话的 SDK 元数据读取。不能调用可能自动恢复的 runCommand/fs 来探测一个已停止实例。
- 保留固定代理和出口校验、一次性接管票据及 WebSocket 到期撤销。没有账号授权或有效平台节点 Key 时，不启动真实账号运行。
- 停止计算不等于零费用：持久化快照可能继续收取存储费用，保留与删除策略需明确。

## 当前验证范围

已在真实 Vercel Sandbox 的 Ubuntu 环境安装并启动 Docker，并成功执行无网络的 hello-world 容器；实例已删除。这证明 Docker 执行基础，不证明完整 worker 或真实账号可用。

`scripts/test-browser-sandbox-runtime.mts` 是显式执行的实时验证，不接入普通 CI 自动计费。它只上传已提交的运行代码和固定上游源码，构建镜像，运行现有合成镜像 smoke，再停止／恢复 Sandbox 校验合成卷标记，最后删除实例和测试快照。它不包含真实账号、代理密码或客户资料。

首轮验证暴露的兼容性差异：当前 SDK 按 name 标识持久实体；快照有效期最小一天；默认工作目录不能硬编码为旧 runtime 的目录。脚本使用已核实的 name、最小有效期和明确的归档路径。失败轮次不记为通过。

## 待实现与验证

平台管理操作和持久化调度状态、按需唤醒与空闲回收、实际 HTTPS/noVNC 接管、并发/撤销恢复回归、账号 profile 一致性和部署接入均未完成。镜像或合成卷验证通过不能代替上述结果，也不能关闭 #332。

## 真实基础验证结果（2026-09-08）

使用 PR 323 的 `e85756cca77d7573e652c5b22e6b68e3275f690e` 与固定上游 `e5a36f5cd0332fde6597de474329a308a53a0716`：

- Sandbox 内安装 Docker/Compose，通过 Agent、Camofox 基础层与浏览器镜像的实际构建。
- 原 `test-browser-images.sh` 全部通过，包括 native Node 24、浏览器/Node 22 依赖、Camofox health、独立看门狗终止与 Compose 配置。未向真实 Facebook 发请求。
- 创建只含 SYNTHETIC_PROFILE 标记的 Docker named volume，正常停止 Docker 和 Sandbox，恢复同一 Sandbox 并重启 Docker后，原镜像可用且标记逐字一致。
- 测试结束成功停止并删除 Sandbox 及测试快照，没有留下常驻计算。首轮失败修正后才取得上述结果。

这不是已登录浏览器的 cookie/加密历史恢复验证，不证明跨重启发布回执、固定代理或 noVNC 外网接管。后续平台生命周期实现仍须满足上方按需契约。

## 按需退出与整体停止（实现基础）

Agent 新增部署级 `BROWSER_NODE_ON_DEMAND=1`。连续成功领取到空队列且本地无运行任务时，默认 30 秒后关闭网关并退出。收到任务或发生领取错误会重置空闲计时；旧 VPS 默认模式不因空队列退出。按需部署必须使用不自动重启的 Agent 容器，不能套用 VPS 的 `unless-stopped` 重启策略。

`stopIdleBrowserSandboxSession` 接收固定的 SDK Session，而非会自动恢复的 Sandbox facade。它核对 sessionId 和 Agent 的节点标签，活动 Agent 不停止；已退出 Agent 会先清理该节点的容器、停止 Docker，再停止整个会话。清理失败也尝试停止计算，但返回未确认状态，不能据此清除旧租约或记为正常释放。旧会话回调不会停止新会话。

验证：5 项空闲策略/实际 Agent 子进程回归、6 项 session 停止/身份/失败分支回归及既有 65 项节点回归通过。实际 Agent 使用本机 HTTPS broker 和 Docker 协议夹具；没有真实账号。实时 `test-browser-sandbox-idle.mts` 在 Vercel 内安装 Docker，构造退出的无网络合成 Agent 容器，再调用真实回收函数；提供方确认 VM 已停止，重复检查保持相同 sessionId 且未恢复。测试实例已删除。类型与格式检查通过。

上述函数尚未接入平台调度，不能宣称生产 worker 已自动按需运行。仍需持久化操作记录、授权校验、唤醒入口与 HTTPS 接管集成。

## 持久化启停协调（2026-09-10）

`0034_browser_sandbox.sql` 与 `sandbox-lifecycle.ts` 新增数据库协调层。登记仅创建元数据；空队列或已撤销节点不启动。启动和停止先锁定同一 broker 节点，再写入唯一操作 ID。只有赢得操作的调用方可以在事务提交后派发云端操作。请求失败或超时保留原操作并进入 unknown，不能据此另建实例；恢复必须查询原实例后确认结果。

回收以已退出 Agent 的确切 sessionId 为前提。新任务即使在停止期间进入，也保留在原队列，等旧会话停止后再启动；旧操作的迟到结果不能覆盖新的操作。这些辅助函数只供服务端事务使用，调用方仍须校验用户／任务授权，不能直接作为 HTTP 或 Action 入口。

真实 PostgreSQL 回归覆盖 12 个并发启动及停止请求、空队列、旧回调、未知结果恢复、停止期间新增账号任务和撤销后拒绝启动。类型和迁移检查通过。这里没有派发 Workflow、调用 Vercel SDK 或保存节点密钥；生产接入仍未完成。

## 托管节点登记与密钥（2026-09-10）

管理命令新增 `create-sandbox`，经现有 Server Action 的身份检查和事务内会话／权限检查。当前入口要求显式 `BROWSER_SANDBOX_ENABLED=1`，自动派发完成前不要在生产启用。登记只分配一个浏览器的默认容量并创建停止状态记录，未提供公网网关时保存 null，不编造接管域名。

节点专用密钥以 AES-GCM 加密保存，使用现有部署密钥环，但以独立 `node-access` 用途和节点 ID 绑定，不能挪作账号密码或复制到其他节点。创建、查询和轮换均不向页面返回托管节点密钥；撤销清空密文并使旧密钥失效。创建、列表查询、轮换和撤销均未调用云端 SDK。真实数据库回归已验证上述授权、隔离和不启动行为；既有浏览器发布、登录及收件回归通过。

尚需实现云端运行配置、派发／恢复调度、网关地址回填、按需管理 UI 和 HTTPS 接管。这个入口不是已完成的生产部署。

## Sandbox 内启动配置（2026-09-10）

`compose.sandbox.yaml` 定义单个 Agent，明确 `restart: no`、禁止拉取镜像、空闲 30 秒退出，节点密钥使用只读文件挂载。Agent 带节点及操作标签，供平台识别对应会话。恢复逻辑因此改为只停止同时具有节点和任务标签的浏览器容器，避免把 Agent 自身当成遗留浏览器停止。

`start-sandbox.sh` 只在已授权且正在运行的 VM 内执行。它检查 root 所有的 0600 配置／密钥文件、节点及操作标识和不可变本地镜像 ID，通过文件锁串行启动。相同操作对应的 Agent 已退出时，不再次启动；不同操作遇到仍运行的 Agent 时要求协调，不能强制替换。脚本不删除 profile 卷。平台生成运行配置和实际调用该脚本的部分仍待接入。

实际 Agent 子进程的恢复过滤与空闲退出回归、会话停止回归共 11 项通过，既有 65 项节点回归通过；启动脚本语法通过检查。新增 Compose 配置验证随双架构镜像 CI 执行。以上尚不证明新启动脚本已在真实 Sandbox 中完成端到端运行。

## 实测发现的资源控制兼容问题（2026-09-10）

首次实际调用启动脚本失败：未初始化嵌套 cgroup 时，Docker 创建的层级进入 threaded 模式，带内存上限的 Agent 无法启动。无资源限制的 hello-world 通过不能证明此配置可用。失败实例已删除。

`prepare-sandbox-cgroups.sh` 采用 [Moby 的嵌套 cgroup 初始化方法](https://github.com/moby/moby/blob/master/hack/dind)，在专用 VM 的首个 Docker 容器之前把现有进程放入子 domain，并启用内存及线程控制器；没有移除容器限制，也不操作 VM 命名空间之外的层级。已经进入不兼容模式时拒绝继续。重试有次数上限，不能永久阻塞启动。

独立真实 Sandbox 探针已验证修复前限内存容器启动失败、初始化后相同配置成功，容器内核 `memory.max` 实际为 67108864（64 MiB）。探针实例均已删除。恢复时 cgroup 是新内核状态，必须在重启 Docker 前重新初始化，不能把文件系统快照当成已恢复资源控制配置。

`test-browser-sandbox-start.mts` 使用已提交 Agent 源码、空队列 HTTPS 合成 broker、临时节点 Key 和不可变本地测试镜像，实际执行启动脚本。它测试重复启动、Agent 空闲退出、HTTPS 接管静态资源及整机回收，不发放真实账号任务，不证明浏览器 profile、代理或 WebSocket 接管。

使用 `8b9bed1` 的完整合成实测通过：Agent 正常启动，容器内核内存上限为 536870912（512 MiB），重复调用返回 already-running，公开 HTTPS 地址可以访问 `/viewer`；空闲退出后同一操作返回 already-exited，没有重新启动。随后真实 session 回收函数停止整个 VM，元数据确认 stopped 且 sessionId 未变。最后删除实例和测试快照，清理完成后才写入成功结果。生产派发与实际业务接管仍未完成。

## 私密配置下发与网关绑定

`startBrowserSandboxRuntime` 对固定的运行中 Session 下发私密配置。密钥只进入 0600 文件，不出现在命令参数；过期会话不执行 I/O。启动和部分上传失败均尝试清理临时文件，清理未确认时不能返回成功。脚本在文件锁内检查已存在 Agent，使用校验后的 Compose 配置快照启动，避免校验和执行读取不同配置。

`3413027` 的真实 Sandbox 测试已通过，成功结果文件在清理完成后写入：私密下发、重复操作、临时文件清理、512 MiB 内存上限、HTTPS 静态页面、空闲退出及整机回收均通过，测试实例和快照已删除。6 项失败／会话隔离单元回归通过。

数据库新增 `bindBrowserSandboxGateway`，将提供方 Session 和 HTTPS origin 在同一事务中绑定到当前启动操作。必须先绑定再允许 Agent sync，不能等 Agent 启动成功后才提供它启动所需的地址。旧操作、另一会话和已撤销节点不能回填；未绑定会话不能结算为 running。真实数据库回归覆盖这些边界。云端派发调用方和部署仍待接入。

## 派发前重新授权

`authorizeManualSandboxStart` 为手动接管派发提供事务内检查：部署开关、当前启动操作、节点和管理员状态、仍有效的用户会话、15 分钟内未取消的排队任务、账号启用及凭据版本、代理配置。仅全部符合时解密节点 Key，并核对现有哈希。调用方须在创建／恢复前检查，并在向 Agent 下发密钥前再次检查；结果不得返回页面或持久化到 Workflow 历史。

真实数据库测试覆盖旧操作、过期请求、无效会话、停用账号、凭据变更、取消任务、禁用管理员和撤销节点。现有发布／收件／登录数据库回归通过。这只是手动需求的授权入口；后台到期发布和收件必须接入各自的当前授权与调度逻辑，不能以手动任务的许可替代。自动派发仍待连接。

## 提供方派发边界

`provisionBrowserSandbox` 调用当前 SDK：首次启动从部署配置指定的已审核无凭据模板快照创建固定节点名称，2 vCPU，仅开放 9400，单次运行上限 20 分钟，保留最近一个快照（30 天存储期限）。正常空闲回收仍必须主动停止 VM，上限不是常驻许可。恢复先只读确认原持久实例已停止，再恢复同名实例；运行中、过渡状态、缺失快照或模糊错误都不新建替代实例。`inspectBrowserSandbox` 仅使用 resume:false。

`0036_browser_sandbox_dispatch.sql` 与 `claimManualSandboxDispatch` 在提供方 I/O 前持久化派发标识并标记已尝试初始化。相同操作只允许一次派发；首次创建响应丢失后不能重新走首次创建，必须查询原名称恢复。派发结果只含节点、操作和模式，不包含密钥。真实数据库验证 12 个并发派发请求只有一个赢家，重复领取被拒绝；4 项提供方夹具测试覆盖模板参数、只读检查、恢复、快照缺失和模糊响应，不代表提供方创建模板的实测。

当前仍没有调用上述函数的持久工作流，也尚未准备生产模板。创建／恢复与运行配置、授权复核、失败清理及正常停止的完整调度连接仍须完成。后台任务、实际 profile 和 WebSocket 接管验证也未完成。

## 单次派发控制器

`dispatchManualBrowserSandbox` 将数据库领取、提交后授权检查、提供方创建／恢复、网关绑定、凭据释放前再次授权、运行配置下发及 running 结果写入串成一次服务端执行。部署模板和镜像配置须先通过校验，不能占用派发后才发现配置缺失。正常结果只返回状态和会话 ID，不能把密钥或 SDK 对象写入工作流历史。

提供方返回会话之后的失败会尝试停止该确切会话，并保留 unknown 供后续核实；清理失败不能记成正常释放。提供方响应丢失时没有可靠会话对象，只记录待核实，不按名称盲目停止或再次创建。5 项控制器回归验证顺序、重复领取、授权撤销、网关／启动／结算失败及无法确认停止；使用可控依赖夹具，尚非完整数据库与云端集成测试。

持久工作流仍需负责触发此控制器、监测正常空闲退出、恢复 unknown 和调度剩余队列，生产模板也尚未准备。生产开关继续保持未启用。

## 持久监测工作流

`runManualBrowserSandbox` 新增一次手动启动操作的工作流：在服务端 step 调用派发控制器，成功后每 15 秒通过可挂起的 Workflow sleep 安排一次会话监测，最多 25 分钟覆盖提供方 20 分钟上限及停止延迟。不延长 Sandbox 超时，不在 VM 内保活。

`monitorBrowserSandboxSession` 先核对数据库会话，再用 resume:false 获取提供方状态。活动 Agent 继续运行，退出 Agent 通过已有回收函数停止整个会话；再次读取提供方确认 stopped 后才在事务中记录停止并清空网关地址。旧会话不能操作新会话。停止记录不释放浏览器租约或改写发布回执，恢复时仍须按原协议核实。

5 项监测单元回归通过。真实数据库回归验证停止记录、清空网关、保留任务队列及重复旧会话监测拒绝；原发布／登录／收件回归通过。工作流编译成功（3 workflows）；完整本地 Next 构建因共享 node_modules 符号链接超出 Turbopack 根目录而失败，完整构建交由 CI 验证，没有关闭 Turbopack 或修改编译器配置。

工作流尚未挂到用户入口和恢复调度。派发响应丢失／step 结果丢失的恢复、撤销后的及时停止、剩余队列唤醒、后台任务调度、生产模板与真实接管仍待完成。

## 撤销后回收（2026-09-11）

监测时重新读取节点状态和当前管理员权限／禁用状态。撤销时不等待 Agent 空闲：先核对同一 Session 和节点标签，给予 Agent 60 秒正常停止窗口，再清理残留容器、停止 Docker 和整台 VM。任何清理错误仍尝试停止已捕获会话，但不能报告干净释放。活动但未撤销节点仍走空闲检查。

14 项停止／监测回归通过，包括活动 Agent 撤销、错误节点和旧会话拒绝。真实数据库回归通过：禁用管理员后选择撤销路径、确认提供方停止后写入 stopped、清空网关且保留队列。提供方部分使用夹具，未执行真实账号撤销。类型检查通过。

上一提交 `b32cd02` 的完整 CI 生产构建已通过，验证新增 Workflow 可构建；浏览器预览测试仍发现重复 DOM 匹配。测试现限定可见表单并断言只有一份，仍需新 CI 结果，不把此前失败记为成功。生产接入与异常恢复仍未完成。

## 启动成功回执丢失恢复

重试先恢复已记录的会话，再按需读取新启动配置。即使部署配置缺失，现有会话仍能继续监测和回收；未启动的新操作仍须通过完整配置校验，且校验失败不领取派发、不创建实例。

派发控制器先读取同一 `dispatch_operation_id` 已记录的运行／停止中会话。若数据库已经记录启动成功而 Workflow step 回执丢失，重试返回该会话 ID，让工作流继续监测，不重新创建或恢复 VM、不下发密钥。领取竞争失败后也再查一次，覆盖另一执行者刚完成写入的情况。停止后及不匹配的操作不返回会话。

## 已绑定会话的异常启动回收

工作流遇到未确认的派发结果时也继续安排观察。`recoverBrowserSandboxDispatch` 对数据库已经绑定会话 ID 的异常启动执行回收：`unknown` 可立即处理，`starting` 必须至少五分钟未更新。事务先按节点、Sandbox 顺序加锁，核对原派发和操作 ID，再把原操作改为停止操作，阻止旧控制器继续绑定网关、领取密钥或写入启动成功。

回收只通过 `resume:false` 查询提供方并操作相同的已捕获会话。先尝试正常关闭 Agent 和浏览器；若启动失败导致 Agent 不存在或无法排空，仍停止该会话，随后读取提供方确认 `stopped`，才清空数据库会话和网关。队列、租约及发布结果不变，后续 Agent 仍须核实浏览器恢复情况。恢复中的停止操作不会被当成成功启动回执重放。

单元测试覆盖缺失 Agent、停止响应丢失、旧会话、过渡状态、已停止实例和并发启动成功；真实 PostgreSQL 覆盖未绑定拒绝、五分钟等待、旧控制器隔离、未知状态重入及队列保留。未绑定会话 ID 的提供方响应丢失仍待实现核实路径，不凭名称猜测会话，也不创建替代实例。本轮没有启动云端实例，生产开关继续关闭。

6 项控制器回归及真实 PostgreSQL 回归通过，验证已记录会话重放、错误操作拒绝和停止后拒绝。该路径只解决数据库已经成功记录的情况；提供方创建响应丢失或尚未写入 running 的操作仍需核实与恢复，不能宣称 unknown 恢复已完成。
