# MVP1 video pre-click verification, 2026-09-24

Issue #383 remains open pending application-managed private video publication and a persisted receipt. This report contains no private account identifiers, page URLs, credentials, or source media.

The production video profile is still `textOnly: true`. In a disposable copy of the stopped managed Sandbox, using its copied persistent Firefox profile and the reviewed proxy, the browser image was rebuilt from pinned Camofox-browser commit `79d425be26743883a06613eaa3be5e38e7ab5409` with the exact-input upload patch. The in-memory copy of the profile allowed video for this diagnostic only. No publish click, application authorization, or production profile change occurred.

The real Facebook composer exposed two file inputs globally, one outside the active composer. An upload to the first global input would target the wrong control. The scoped upload selected the unique input inside the reviewed composer and attached a 21,676-byte, self-generated SMPTE color-bar MP4. Facebook replaced the composer and reset its audience; the driver rechecked acting identity, restored `Only me`, and entered the exact test caption. The active media composer displayed one blob-backed video, no filename marker, and no retained selected file in its input. Reading the blob through Firefox and computing SHA-256 matched the source MP4. The final no-click preview showed one verified attachment and an enabled publish button.

The driver now checks the video blob digest before authorization and again in the same page evaluation that performs the single publish click. A missing, ambiguous, unreadable, oversized, or mismatched preview fails before the click. Synthetic browser tests cover a correct blob, a wrong blob before authorization, and a changed blob after authorization; 31 driver tests passed. Native-profile tests (7), the social worker executor test, the video publication policy test, and TypeScript checking passed. These checks establish the media preview contract, not a production delivery or receipt. A separate private production post and durable application receipt are still required.
