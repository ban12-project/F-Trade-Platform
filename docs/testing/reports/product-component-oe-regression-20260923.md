# 产品组件 OE 归属回归 — 2026-09-23

关联 #392。提示词 1.1.1 明确：所选产品的 OE 只能来自其自身 OE/OEM 栏，组件栏中的 OEM 标签不能填补整件空 OE。纠错提示同步强调该规则；生产来源校验、预算、评分器和人工 Gate 不变。

代码 `707c8ac`，Kimi K3 / json_schema，20 个既有合成场景各 3 次，75 秒预算、最多一次纠错。新的完整批次 **60/60 首次通过、60/60 最终通过，0 次纠错、0 次超时**；c-02 与 d-02 各 3/3 首次通过。P50 31.828 秒，P90 43.241 秒。运行健康检测有效，最大事件循环间隔 3122 ms，无时钟倒退和超预算试次。已知输入 269694 tokens、输出 79009 tokens；没有账单证据，不估算金额。

[白名单结果](product-component-oe-regression-20260923.json)经 `validate-product-model-selection-report.ts` 重算统计与完整性校验通过。原始响应及端点配置仅留本地忽略目录。旧 300 次比较和失败结果保持不变。

这是针对已见错误修订提示词后的回归，不是独立留出验收，也不证明所有真实目录、图片/OCR、跨语言和组件版式均通过。模型原始 blockers 与应用重算规则的一致率仍为 0；该指标依既定协议不参与严格通过，但不能将它称为模型缺失项识别已完全正确。应用最终字段、证据、缺失项和状态由冻结评分器验证。

本地来源证据及 product-output 测试通过。首次项目类型检查受未跟踪的上游 Camofox 调试文件影响；同一提交干净检出类型检查通过。没有放宽 tsconfig 或修改第三方源码。当前 PR 的代码 CI 通过后追加本报告，追加提交仍须通过 CI 后合并。

结论：本配置在该合成回归集通过，支持交付提示词修复。#392 及 MVP1 不据此整体关闭，真实授权资料留出、渠道生产验收及人工 Go/No-Go 仍待完成。
