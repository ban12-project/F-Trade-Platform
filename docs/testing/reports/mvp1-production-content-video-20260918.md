# MVP1 生产内容与视频补测 — 2026-09-18

关联 #367、#381，补充 [非 DM 验收](mvp1-non-dm-acceptance-20260918.md)。PIN / DM 按用户决定搁置。应用仍为已验证源版本 `f4e29f46dbc0540199984f570867bfd66ad179fe`；#380 仅合并报告，没有部署应用代码变化。

## 结论

通过生产已登录 UI，在独立 TEST ONLY / MOCK 项目完成：支持标签的 CSV 上传与实际模型产品导入、产品模拟审核、实际模型营销内容生成与模拟审核、授权参考片段上传、实际模型剪辑、编辑、云端合成、私有预览、成片模拟审核及下载。

产品内 Facebook 受控发布仍未通过生产验收：渠道/私有页面契约未配置，没有应用发布回执。此前手动文案与视频地址的验收仍成立，但不能替代产品发布队列。整体验收不是全部通过，也不构成人工 Go。

## CSV 对照与产品门禁

- 首次测试 CSV 使用下划线字段名，模型流在约 19 秒后标记 completed，但产品字段为空，保留四项阻塞。空记录未批准。
- 对照保持七项测试值不变，仅改为已支持的 Product name、Product type、Internal SKU、OE、Application、Vehicle brand、Vehicle model 表头。七项均逐字导入并绑定来源位置，未新增规格或商业事实。
- 代码的已审核标签约束与这个差异一致；没有保存模型原始输出，因此不将所有字段拒绝原因进一步断言为模型错误。首次输入的验收失败保留，#381 跟踪支持标签提示及零字段结果的可操作反馈。
- 模拟审核备注明确记录自动化测试、非真实人工/工厂批准、非销售授权。批准仅让 MOCK 产品进入后续测试，不认证真实工厂事实。

## 生产营销内容

- 使用实际已配置模型，目标明确为内部 MOCK 验收。草稿保持精确测试产品名称及 OE，明确为 synthetic、非客户材料；没有新增适配、认证、供货、价格、性能或交期承诺。
- 视觉方案为抽象背景和文字卡片，没有生成产品结构图。
- 通过实际 UI 保存待审记录并完成有明确测试说明的模拟批准。事实引用仍对应该 MOCK 产品；没有触发发布。
- 本次为一条生产内容样本，独立英文介绍的 20 条核验仍使用前一报告的受控证据，不合并为同一次运行。

## 生产视频

1. 将现有用户授权的私有测试范围记录为项目证据，上传已有带 TEST ONLY 标记的 12.033333 秒参考衍生片，3,701,465 字节。未上传大型原始 MOV，也不声称画面对应 MOCK 产品。
2. 实际 AI 剪辑任务一次成功，形成两个片段。原草稿含通用经销商字幕及 CTA，与内部验收目标不一致；这是需要编辑的质量问题，不能将原草稿称为无需修改即可用。
3. 在编辑器一次保存修改：两段新增字幕设为无字幕，CTA 改为明确的 TEST ONLY / MOCK 内部审核文案。原素材与来源不变。
4. 合成任务一次成功，约 324 秒，进入 VIDEO_REVIEW_REQUIRED。实际成片 9.706667 秒，1080×1920、30 fps、H.264/AAC、7,693,036 字节；时间线 9.626 秒。导出预设验证通过，平台接受状态仍为 not_evaluated。
5. 生产页面加载私有预览，视频 readyState=4；等待 seeked 后核对 2 秒画面及末尾 CTA。测试名称/标记可见，未发现新增事实字幕。首次截图在 seek 完成前显示过渡画面，不作为稳定抽帧判据；不宣称完整逐帧、HDR 或所有输入格式验收。
6. 明确的模拟成片审核后状态为 VIDEO_APPROVED，显示 MP4 与清单下载。审核不自动发布，不是真实工厂或人工业务批准。
7. 登录状态下预览、MP4 与清单均 200；预览和 MP4 的 7,693,036 字节及 SHA-256 相同。清单 reviewStatus=approved、validation.status=passed。三个端点未登录请求均 403。

## 清理与交付

- 所有本次创建的临时云对象共五个已删除：三份 CSV（失败输入、对照输入、测试授权记录）、一个素材视频、一个成片。每个对象均以私有无缓存读取确认不存在。
- 数据库失败记录、模拟审批与任务记录保留，既有真实资料和账号存储未修改。由于对象已清理，本次记录不再代表可用原件、云预览或可投递媒体，不能拿这些记录上线发布。
- 没有新 Facebook 帖子、DM、客户报价或真实工厂审核。公共报告不包含账号、私有 URL、凭据、原始资料或模型正文。
- #380 的首轮 CI 有一项数据库浏览器用例在等待“记录 1”页签时超时（19 项通过、1 项失败）。下载失败 trace 后，仅原样复跑失败 job；复测通过，未增加超时或跳过断言，全部检查通过后 squash 合并为 `22aee34dea3f7762ed8fefd967e08f67b1ecd299`。没有据此声称已修复间歇超时根因。

## 仍未通过或未覆盖

- #381：下划线 CSV 标签的零字段草稿与反馈问题，未修复。
- 产品内真实 Facebook 发布：生产配置与应用回执缺失，未通过；手动发布只能证明人工接管路径。
- 大型原始 MOV、HEIC、HDR、完整逐帧质量不在本轮通过范围。
- PIN / DM 明确搁置；不把搁置视为通过。
- 最终工厂事实、正式报价、交期和 Go/No-Go 由对应负责人决定。本轮验收记录不能代替这些决定。
