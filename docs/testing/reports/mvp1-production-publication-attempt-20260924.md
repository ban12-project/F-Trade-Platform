# MVP1 private text publication attempts — 2026-09-24

Issue #383. None of the three new private text attempts established a successful publication receipt. Do not retry these jobs or mark these posts published without a verified canonical permalink.

The first synthetic, claim-free `Only me` text item (content `d14979d1-e9e4-42b4-bf10-fca611965578`, job `5b01fab2-536a-4e81-8e68-e3aba0b4719f`) was approved and submitted once. Starting from a stopped managed Sandbox, publication demand woke the provider and the node claimed the job. The app then marked the publication `unknown` and paused the channel. A read-only production database check found `authorization_id IS NULL`, `receipt IS NULL`, and no external publication reference. Under the reviewed executor's authorize-before-click order, this run did not execute its controlled publish click. The historical post previously reconciled by a human is a different item.

The second synthetic item (content `ea46ebe7-76ef-48ba-b11c-9d1c6e4b80d0`, job `780531be-57f8-4f99-96bc-2674ae5fab83`) was also approved and submitted once after an audited channel resume. It too ended `unknown`/paused without an authorization, receipt, or external reference. The stopped Sandbox had received a reviewed diagnostic Agent image, but startup config from the still-active application deployment replaced its image selection with the old image. That run therefore produced no pre-click stage log. The original jobs remain paused and have not been replayed.

Short-lived forks of the stopped production Sandbox reused copied native Firefox account storage and the same proxy. Without contacting the production node broker or clicking publish, the browser verified account identity, currently visible post baseline, exact text, `Only me` audience, zero attachments, and `readyToPublish=true` for both bodies. A later isolated check also passed the browser-side fixed-egress-IP probe after preparing the second body. These checks narrow the likely failure to a transient production step or the authorization path, but do not identify a specific failing call. They do not establish absence from all Facebook history.

The fixed-enum diagnostic Agent was built from merged revision `53b1b4b88166e8b4947bbb0e0c7d01dad02dfb1f`, smoke-tested, and placed in a credential-free Sandbox template. Production template and image settings were updated together, and the same revision was redeployed to `ftp.ban12.com`. A later interactive wake started exactly this Agent image and observed the saved Firefox session as login-ready without requiring password, TOTP, or PIN input. This verifies the session-first path across another managed Sandbox stop/start.

The broker's previous reconciliation treated every claimed job without a success receipt as `unknown`, including a stopped run that never obtained authorization. Revision `71800a51d9253a7a34c13c95ee66be8698073005` classifies this provably no-click case as failed while retaining `unknown` for authorized or still-live runs. After independently verifying the stopped reviewed Agent, null authorization, null receipt and exact job/content bindings under a transaction, the first two misclassified records were reclassified as failed with audit entries; their jobs remain paused and were not replayed. The channel was not automatically restored.

A third distinct synthetic `Only me` text item (content `22541304-e9bc-4e35-9596-5803ac279818`, job `7a6271c8-a496-4d4f-8de5-959f03c1e331`) was approved and submitted once after an audited channel resume. The new Agent image ran and logged `browser_publication_preclick_failed:baseline`: reading existing post references failed before authorization. The database recorded publication `failed`, job `paused`, null authorization and receipt, while the channel stayed active and the account stayed login-ready. A separate no-click fork using the exact current driver and proxy subsequently read two existing references and passed composer/egress checks. The baseline failure is therefore not yet reproducible; it may be transient. The next change adds one bounded, read-only retry for known transient baseline errors and records only a fixed error code if both attempts fail. No click retry is introduced.

Video publication and real inbound DM acceptance remain outstanding.
