# MVP1 private text publication attempt — 2026-09-24

Issue #383. This report records one production attempt following the persisted-session checkpoint. It does not establish a successful publication receipt.

The account's existing Firefox profile was ready before the attempt. The synthetic, claim-free text draft received Gate 01 approval under the user's current-task authorization. The channel was restored with an audit reference, then the approved `Only me`, text-only item was confirmed and submitted exactly once. The application created one queued publication job. Starting from a stopped managed Sandbox, the provider woke, the browser node claimed and ran the job, and the Sandbox stopped afterward. This establishes publication-demand wake, but not successful delivery.

The new browser run ended with `status=unknown`, `failure=failed`, and no `publicationOutcome` in the owner's node state. The application marked this publication `unknown` and paused the channel. No signed success receipt or canonical permalink was recorded. The original prior post's human reconciliation remains separate. The current item must not be resubmitted or manually marked published from these signals.

A short-lived diagnostic fork of the stopped Sandbox reused a copy of the same Firefox profile and fixed proxy. No Agent connection to production was started in the fork. Its browser passed account identity, visible-post baseline, exact-text composer preparation, `Only me` audience validation, no-attachment preview, and `readyToPublish=true`. The exact new text was absent from the fork's currently visible profile posts. This is a bounded observation, not proof that the post is absent from all Facebook history. The diagnostic browser never clicked publish; the fork and its snapshots were removed.

The production failure could have occurred after that preparation, during short-lived authorization, or through a transient page or transport state. The old executor collapsed all failures before a click into `failed` without a stage code, so the current production record cannot identify the failing stage. A fixed-enum stage diagnostic is required before another independently authorized test item. The channel must remain paused until the current external result is checked and the failure is understood. Video publication and real inbound DM acceptance remain outstanding under their separately reviewed scopes.
