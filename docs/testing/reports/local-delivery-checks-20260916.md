# 本地交付检查

关联 #340、#341、#342、#343、#26。本记录汇总四个后续分支当前源码的本地检查，
不等同于远端 CI、部署成功或 MVP1 Go 决定。

使用 Corepack 提供仓库声明的 pnpm 11.24.0，通过独立临时 shim 供子进程调用，
没有修改仓库 packageManager 或全局 pnpm 配置。Python 使用已有项目虚拟环境。

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| `scripts/validate_repository.py` | 37 项通过，退出 0 | 契约/夹具、数据库基线、工作流、合成演示、导入、渠道/回复规则、报价/交期/RFQ/跟进门禁、仓库保管和文档链接 |
| `pnpm check` | 退出 0，621 个文件；243 条 warning、7 条 info | Biome 检查通过，但不是零警告 |
| TypeScript | 最近一轮 `tsc --noEmit` 退出 0 | 本机类型检查，不替代远端构建 |
| 文档镜像构建目录 | 合成隔离回归和实际仓库 Dockerfile/模块打包均通过 | 防止私密工作区文件进入构建上下文 |
| `git diff --check` | 通过 | 当前改动的空白格式检查 |

构建目录回归增加了对实际仓库 Dockerfile 的检查：即使合成夹具仍通过，真实 COPY
声明与模块白名单不一致时也会失败。测试只复制受控代码，不复制参考资料。

其他验收证据分别见[目录浏览器链路](catalog-browser-chain-20260916.md)、
[视频修订](reference-video-intake-20260916.md)、[扫描套件目录](scanned-kit-layout-20260916.md)
和[固定镜像](document-image-20260916.md)。这些是不同运行，不合并成一次未经执行的全流程。

## 未完成的交付与验收

- GitHub 凭据仍缺少修改工作流所需的 workflow scope，后续分支尚未推送，也未取得远端 CI。
- 新镜像已验证，但 Preview/Production 固定摘要配置仍未切换。
- 完整 A–D 产品字段与图片条件、真实工厂事实和实际人工 Go/No-Go 不能由上述合成检查替代。
- 先前 MOCK 产品、审核、报价、商机及回复入队结果保持模拟分类，不表示真实发布或投递。
