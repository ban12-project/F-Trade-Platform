# PR 323 迭代状态与阻塞

核对日期：2026-09-08。范围为研发迭代；PR 保持 Draft，不代表部署、真实账号操作或最终验收授权。

## 本轮完成

已保存登录的 UI → Server Action → 一次性授权 → Agent 执行器 → 专用浏览器填字段路径已接通。只有审核过且未过期的账号页面配置才能启用。成功填字段、拒绝和未知结果分别记录；领取前拒绝也关闭该次授权。密码不返回页面、不进入运行检查点或容器环境。填字段不提交登录、不完成 2FA、不自动标记账号 ready。

本地使用 Node 24 和纯合成夹具：64 项节点回归、27 项 Chromium 登录插件／执行器场景、真实 PostgreSQL 发布／收件／登录授权回归通过；实际 Next.js 页面／Server Action／节点 HTTP 的填充状态流程通过。离线打包正向与缺失文件反向检查、TypeScript 通过。页面测试模拟远程查看器及节点填充回执，执行器测试模拟平台传输；不能把两段结果合称真实 Docker 全链路。新增配置测试首轮引用格式错误导致 1 项失败，修正夹具后 64 项通过。

## 当前阻塞与下一步

| 范围 | 当前证据与缺口 | 下一步 |
| --- | --- | --- |
| PR 323 合并验证 | GitHub 作业未启动；注释明确为账户付款失败或 spending limit。不是已执行测试失败。 | 账户维护者恢复 Actions 后，重跑当前 head 的 CI、双架构镜像构建与 smoke；本地通过不能替代。 |
| #322 浏览器运行 | 本地没有 Docker，但已有 Podman 5.8.3 / amd64 虚拟机；同一持续会话中能启动并连接。实际 Agent 构建被 Docker Hub 拉取超时阻塞；固定 Camofox 源码已通过 SSH 获取并离线校验。Node 22 浏览器镜像、noVNC、持久化会话、固定代理及日志隐私仍未验明。 | 先在隔离容器以合成数据验证运行，再依据授权安排账号所有者验证；不直接启用真实发布。 |
| #322 页面契约 | 可配置发布与收件驱动已实现，但没有审核过的真实登录／发布／Messenger 页面规则、历史／PIN 行为和新帖子回执证据。 | 固定具体账号路径和版本，提供有期限的审核契约；未经验证的能力默认关闭。 |
| #322 收件覆盖 | 当前只证明有明确起终标记的可见窗口；最多 40 页、200 会话、200 入站消息。没有跨轮持久化游标。 | 确定所需覆盖范围；若要超出窗口持续补采，另做持久化进度和恢复契约。不能声称全量历史。 |
| #320 项目访问 UX | 独立 Draft PR #326（f9563bd）已实现统一不可用页；开发模式 8 项和生产模式 13 项回归、生产构建通过。 | 等待远程 CI 与交付；不并入 PR 323。 |
| #268 / #307 资料入库 | 合成产品及预置证据链路已有测试；完整私有浏览器上传、原件回读与 A–D 图片覆盖仍未完整验证。 | 用已授权测试资料补上传和证据回读；原件与正文不进 GitHub。 |
| #246 视频 | 实际私有预览、目标编码验收仍需独立证据。 | 用获授权媒体生成成片，检查 9:16 H.264/AAC 及权利／事实来源。 |
| #126 / #318 导出规则 | 项目导出预设不等于特定平台路径接受规则；来源版本差异仍开放。 | 固定目标 API／账号类别及规则版本，独立列出已测与未知属性。 |
| #122 发布政策 | 保留期、预算／额度责任人、媒体权利范围、具名审核人尚未补齐。 | 由业务负责人形成版本化决策；不从 DM 保留期推断视频政策。 |
| #6 真实工厂资料 | 真实 SKU 授权、保密等级、A–D 分组未齐；仅阻塞真实工厂验收，不阻塞已授权 mock 迭代。 | 工厂／资料负责人提供来源及允许使用范围。 |
| #26 / #32 最终验收 | 按用户要求暂缓。 | 待迭代与独立验证完成后安排，不以局部通过提前关闭。 |

#325 的文本确认版本／摘要绑定已在 PR 323 实现并有数据库回归，仍须随 PR 的 CI 与交付流程核对后关闭，不能仅因 Issue 仍开放就认为功能未实现。

来源：PR 323、开放 Issue 正文及代码；CI 启动失败证据：Actions run 34191489452，job 101950305467。上述其他 Issue 的缺口来自其当前记录，不声称本轮重新执行了每一项外部验证。

本次恢复检查补充：无回执的登录请求在过期／中断后不再无限等待；仅推断 refused/unknown，保留晚到的有效回执。65 项节点回归、迁移数据库组合和实际 Next.js 页面状态回归通过。真实容器限制不变。


## 容器环境复核与独立迭代补充

2026-09-08，针对 PR 323 源码 `a54fb20`，发现主机已有停止的 Podman 虚拟机。首次启动报告成功后进程退出；在同一持续会话中执行启动、构建和 smoke，虚拟机维持运行，Podman API 可查询，Agent 构建进入 `node:24-trixie-slim` 拉取阶段。因此不能继续将“没有 Docker”解释为“没有任何本地容器路径”。

本轮构建最终退出 125：Docker Hub registry 443 连接超时，未产生目标镜像，后续 smoke 未执行。固定公开 Camofox 提交获取退出 128（HTTP/2 framing error）；主机对 GitHub 的独立 15 秒请求也超时。没有更换基础镜像、上游版本或代理来掩盖失败。Podman 兼容性、双架构和浏览器运行行为均未证明。结束时确认虚拟机停止，无本轮测试容器运行。

独立草稿交付进度：PR #327 已补文件大小及码率测量，五种预设的合成 FFmpeg 渲染通过；这不解决平台接受契约。PR #329 修复工作台视频预览／下载／清单缺少项目成员校验，并验证真实数据库入口的事实变更、证据撤销、素材授权过期／撤销／用途限制与恢复；合成元数据及模拟媒体读取不证明真实文件传输。两个 PR 均不并入 PR 323，也不构成最终验收。

后续源码获取已解除：通过 Git SSH 读取同一公开仓库的固定 SHA `e5a36f5cd0332fde6597de474329a308a53a0716`，保留原 HTTPS origin。准备脚本现在在完整提交对象已存在时跳过 fetch，仍校验固定 SHA、原始 origin、干净工作区及检出后的 HEAD。实际固定源码离线准备通过；合成 Git 仓库回归证明缓存命中、脏工作区拒绝、错误 origin 拒绝、缺失提交触发 fetch 及可变 ref 拒绝。镜像仓库网络与真实容器 smoke 仍未解决。

固定上游插件契约补充：直接导入该 SHA 的 `lib/plugins.js` 与 `lib/auth.js`，加载项目登录插件；默认无配置不注册端点，配置后注册两个端点，实际全局与路由认证拒绝缺失／错误密钥，有效密钥进入安全拒绝路径，上游加载器未吞掉注册异常。合成回归已通过并加入镜像 CI 的源码准备之后。测试使用轻量请求／响应对象和 Node 24，不启动 Express、Node 22 浏览器、容器或真实账号，因此不替代对应运行验证。
