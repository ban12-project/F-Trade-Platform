# PR 323 迭代状态与阻塞

核对日期：2026-09-12。研发迭代继续，最终验收暂缓。PR 323、326、327、329、330、333 均为 Draft，尚未合并。本文区分代码、合成运行证据与生产验收，不把开放 Issue 等同于功能未实现。

## 已解除的阻塞

- Actions 已恢复：PR 323（`e85756c`）及 PR 333（`1f50ae1`）的生产构建、Playwright、PostgreSQL、合成测试、仓库校验和 amd64／arm64 镜像构建及 smoke 均通过。镜像发布与 release-validation 为跳过，不能解释为已发布。
- 新数据库已初始化：只读核对本地 `.env` 指向的数据库，38 项迁移按 ID 排序后的哈希与本分支完全一致；用户、Sandbox、派发记录均为 0。没有恢复旧业务数据，也没有创建管理员。该结果不证明 Vercel 生产环境连接了同一数据库。
- VPS 不可连接已通过迁移方案绕开，不再是浏览器运行的必要前置条件；Openship 未部署。
- Vercel Sandbox 的真实 Docker／Camofox、完整虚拟机停止后恢复 cookie 与 localStorage、Agent 空闲退出、HTTPS／WebSocket、noVNC 密码认证和远程鼠标／键盘均有合成运行证据。测试虚拟机已删除；未操作真实账号。
- 首次创建响应丢失已有保守恢复：只认匹配节点与操作标签、唯一历史会话及当前会话 ID 的 Sandbox；先绑定并隔离旧操作，再停止捕获的会话。并发及晚到响应回归通过，真实提供商发现测试通过。不代表恢复启动响应丢失也已解决。

## 按需运行与部署边界

PR 333 基于 PR 323，管理型节点注册只写元数据；经授权的手动需求才创建或恢复 Sandbox，空闲时停止整个虚拟机，并设运行时限兜底。恢复前检查节点归属标签、固定 2 vCPU 规格及不超过 20 分钟的时限；元数据缺失或被外部改大时拒绝唤醒。界面已加入名称注册入口及开关控制，不要求用户填写节点密钥。

Vercel 生产环境已配置模板、镜像、平台地址及调度密钥，但 `BROWSER_SANDBOX_ENABLED=0`，尚未重新部署生产环境。五分钟调度入口扫描派发记录，本身不创建常驻浏览器；上线后仍会产生函数调用，停止虚拟机后保留的快照也不等于零存储费用。

独立预览 `https://f-trade-platform-q95996pwm-ban12.vercel.app` 已完成真实云构建；未登录工作台重定向、无调度密钥返回 401。该预览早于最新注册界面与首次创建恢复提交；PR 的 Vercel 成功状态包含 Ignored Build Step，不能作为最新代码已上线的证据。

当前模板到期时间为 2026-09-19 09:13 UTC；若部署晚于有效期，须重建并更新引用。

## 当前阻塞与下一步

| 范围 | 当前证据与缺口 | 下一步 |
| --- | --- | --- |
| 新数据库上线 | 本地连接迁移完整，但尚无平台用户；Vercel 生产 DATABASE_URL 的目标身份未核实。 | 确定平台首个管理员邮箱后使用已有初始化入口；核对线上数据库目标并验证登录。此前 Openship 邮箱不自动用作平台管理员。 |
| #332 平台完整链路 | 真实运行时和独立查看器夹具已通过；实际平台 outbox → Workflow → Agent → 停止回写，以及平台页面签发一次性票据的完整链路尚未验证。 | 用合成账号配置完成授权、调度、查看器与空闲停止的集成验证，再更新最新预览。 |
| #332 中断恢复 | 已记录会话与首次创建丢响应可保守回收；恢复启动丢响应且存在多次历史会话时仍不自动认领。 | 补充可证明属于当前操作的恢复契约与故障测试，无法证明时保持未知并拒绝盲目重启。 |
| #322 真实账号契约 | 固定代理出口、真实登录／发布／Messenger 规则及回执尚无完整验证；当前自动需求入口限于手动请求。 | 单独验证固定代理及带版本、期限的页面契约；真实发布仍须逐帖人工确认。 |
| #322 收件覆盖 | 当前有界可见窗口最多 40 页、200 会话、200 入站消息；没有跨轮持久化游标。 | 按需要定义持续补采和恢复契约，不声称全量历史。 |
| #320 项目访问 | PR 326 已实现统一不可用页，开发及生产模式回归通过。 | 随独立 PR 交付；不是未实现功能。 |
| #268 / #307 资料入库 | 编号、重复记录、页码来源及合成领域流程已有回归；完整私有浏览器上传、原件回读和 A–D 图片覆盖仍缺独立证据。 | 使用已授权资料补上传及回读；原件与正文不进入 GitHub。 |
| #246 / #331 视频 | PR 330 已修复时序并验证合成私有 Remotion 渲染、上传与媒体测量；测试媒体已清理。 | 补获授权媒体和业务审核链路，不能把合成成片当作正式发布验收。 |
| #328 视频权限 | PR 329 已补项目成员校验与数据库授权回归。 | 随独立 PR 交付，不因 Issue 开放而重复实现。 |
| #126 / #318 导出规则 | PR 327 已补文件大小及码率测量；项目预设仍不等于特定平台 API／账号路径接受规则。 | 固定目标路径、来源版本和未知属性，分别验证输出与平台接受契约。 |
| #122 发布政策 | 首发渠道已有决策；视频等保留期、预算责任人、完整媒体权利范围及具名审核人尚未补齐。 | 形成版本化业务决策；不从 DM 保留期推断视频政策。 |
| #6 工厂资料 | 真实 SKU 来源授权、保密等级、A–D 分组未齐。 | 阻塞真实工厂验收，不阻塞已授权 mock 迭代。 |
| #26 / #32 最终验收 | 按用户要求暂缓。 | 不以局部通过提前关闭。 |

#324 的会话授权相关修复及 #325 的文本确认版本／摘要绑定已进入 PR 323 并有回归；仍须随交付流程核对后关闭。

## 证据索引

- GitHub：PR 323、333 当前提交的 checks；上述 Issue 当前正文。本文没有重新执行全部历史业务验证。
- 本地忽略目录中的真实模板停止／恢复证据：`tmp/browser-sandbox-template-test-d23ac3b2-c520-48ad-abab-e508f6ae795c/result.json`。
- Agent 空闲停止证据：`tmp/browser-sandbox-start-cbc08017-c16d-4241-b068-de1d1cee91d1/result.json`。
- noVNC 远程输入证据：`tmp/browser-sandbox-websocket-27c76842-b7d3-47ed-97bc-0d3bfb0f0219/result.json`。
- 首次创建响应丢失发现证据：`tmp/initial-discovery-26980274-fb47-49df-8767-7742ba11f3ed.json`。
- 上述运行证据均使用合成数据；不包含真实 Facebook 登录、发布或私信验收。

恢复前配置防护补充：提供商、控制器和恢复处理共 23 项本地回归通过；包含外部修改 CPU、缺失规格、错误节点标签时只有只读查询且没有恢复调用。该防护不解决恢复响应丢失的会话归属缺口。
